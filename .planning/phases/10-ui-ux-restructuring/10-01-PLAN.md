---
phase: 10-ui-ux-restructuring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/widgets/app_shell.dart
  - lib/features/statistics/presentation/pages/statistics_page.dart
  - lib/features/statistics/presentation/widgets/coming_soon_placeholder.dart
autonomous: true
requirements: [UI-11, UI-13]
user_setup: []

must_haves:
  truths:
    - "User can see Statistics tab in bottom navigation (5th position: Dashboard, Calendario, Prenotazioni, Statistiche, Impostazioni)"
    - "Statistics tab shows placeholder content 'Statistiche in arrivo...'"
    - "Default tab on app start is Dashboard (index 0)"
    - "Platforms tab is no longer in bottom navigation"
  artifacts:
    - path: "lib/core/widgets/app_shell.dart"
      provides: "Main navigation shell with updated tabs"
      contains: "BottomNavigationBarItem.*Statistiche"
    - path: "lib/features/statistics/presentation/pages/statistics_page.dart"
      provides: "Statistics page with placeholder"
      exports: ["StatisticsPage"]
    - path: "lib/features/statistics/presentation/widgets/coming_soon_placeholder.dart"
      provides: "Reusable coming soon widget"
      exports: ["ComingSoonPlaceholder"]
  key_links:
    - from: "lib/core/widgets/app_shell.dart"
      to: "StatisticsPage"
      via: "_pages list"
      pattern: "StatisticsPage"
---

<objective>
Replace Platforms tab with Statistics tab in bottom navigation.

Purpose: Prepare navigation structure for Phase 11 (Statistics Feature) by creating the Statistics tab with placeholder content.
Output: Updated AppShell with Statistics tab, new StatisticsPage with placeholder, reusable ComingSoonPlaceholder widget.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-ui-ux-restructuring/10-CONTEXT.md

<interfaces>
<!-- Current AppShell navigation structure (to be modified) -->
From lib/core/widgets/app_shell.dart:
```dart
// Current pages list (will be modified):
late final List<Widget> _pages = [
  DashboardPage(onCalendarTap: () => navigateToCalendar()),
  const CalendarPage(),
  const ReservationsListPage(),
  const PlatformsListPage(),  // TO BE REPLACED
  const SettingsPage(),
];

// Current bottom nav items (will be modified):
items: const [
  BottomNavigationBarItem(icon: Icon(Icons.dashboard), label: 'Dashboard'),
  BottomNavigationBarItem(icon: Icon(Icons.calendar_today), label: 'Calendario'),
  BottomNavigationBarItem(icon: Icon(Icons.list), label: 'Prenotazioni'),
  BottomNavigationBarItem(icon: Icon(Icons.hotel), label: 'Piattaforme'),  // TO BE REPLACED
  BottomNavigationBarItem(icon: Icon(Icons.settings), label: 'Impostazioni'),
]
```

<!-- Animation utilities for consistency -->
From lib/core/utils/animations.dart:
```dart
class AppAnimations {
  static const Duration medium = Duration(milliseconds: 300);
  static bool shouldReduceMotion(BuildContext context);
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ComingSoonPlaceholder widget</name>
  <files>lib/features/statistics/presentation/widgets/coming_soon_placeholder.dart</files>
  <behavior>
    - Test 1: Widget displays centered icon (bar_chart or analytics)
    - Test 2: Widget displays title "Statistiche in arrivo..."
    - Test 3: Widget has proper semantic labels for accessibility
    - Test 4: Widget respects reduced motion settings for any animations
  </behavior>
  <action>
Create a reusable placeholder widget for upcoming features.

Implementation:
1. Create StatelessWidget with const constructor
2. Add optional title parameter (default: "Statistiche in arrivo...")
3. Add optional icon parameter (default: Icons.bar_chart)
4. Build centered Column with:
   - Large icon (64px) in primary color
   - SizedBox(height: 16)
   - Text title in titleMedium style, grey color
5. Wrap in Semantics widget with label "Funzionalita in arrivo"
6. Add subtle pulse animation (optional, respects reduced motion)

File structure:
```dart
import 'package:flutter/material.dart';

class ComingSoonPlaceholder extends StatelessWidget {
  final String title;
  final IconData icon;

  const ComingSoonPlaceholder({
    super.key,
    this.title = 'Statistiche in arrivo...',
    this.icon = Icons.bar_chart,
  });

  @override
  Widget build(BuildContext context) {
    // Centered icon + text with semantics
  }
}
```
  </action>
  <verify>
    <automated>flutter test test/features/statistics/presentation/widgets/coming_soon_placeholder_test.dart</automated>
  </verify>
  <done>ComingSoonPlaceholder widget exists, renders correctly, has accessibility support</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create StatisticsPage with placeholder</name>
  <files>lib/features/statistics/presentation/pages/statistics_page.dart</files>
  <behavior>
    - Test 1: Page displays ComingSoonPlaceholder
    - Test 2: Page has AppBar with title "Statistiche"
    - Test 3: Page has correct key 'statistics_view' for testing
    - Test 4: Page is a StatelessWidget (no state needed for placeholder)
  </behavior>
  <action>
Create the Statistics page that will host statistics in Phase 11.

Implementation:
1. Create StatelessWidget with const constructor
2. Add Scaffold with AppBar (title: "Statistiche")
3. Add key: const Key('statistics_view')
4. Body: Center with ComingSoonPlaceholder widget
5. No floating action button (Phase 11 will add content)

File structure:
```dart
import 'package:flutter/material.dart';
import 'package:app_prenotazioni/features/statistics/presentation/widgets/coming_soon_placeholder.dart';

class StatisticsPage extends StatelessWidget {
  const StatisticsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      key: const Key('statistics_view'),
      appBar: AppBar(
        title: const Text('Statistiche'),
      ),
      body: const Center(
        child: ComingSoonPlaceholder(),
      ),
    );
  }
}
```
  </action>
  <verify>
    <automated>flutter test test/features/statistics/presentation/pages/statistics_page_test.dart</automated>
  </verify>
  <done>StatisticsPage exists, displays placeholder, has proper structure for Phase 11</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Update AppShell navigation to use Statistics tab</name>
  <files>lib/core/widgets/app_shell.dart</files>
  <behavior>
    - Test 1: Bottom nav has 5 items (Dashboard, Calendario, Prenotazioni, Statistiche, Impostazioni)
    - Test 2: Tapping Statistiche tab shows StatisticsPage
    - Test 3: Statistics tab is at index 3
    - Test 4: Default tab is still Dashboard (index 0)
    - Test 5: No PlatformsListPage in navigation
  </behavior>
  <action>
Replace Platforms tab with Statistics tab in AppShell navigation.

Changes to lib/core/widgets/app_shell.dart:
1. Update imports: Remove platforms_list_page.dart, add statistics_page.dart
2. Update _pages list: Replace PlatformsListPage with StatisticsPage
3. Update BottomNavigationBarItem: Replace 'Piattaforme' with 'Statistiche'
4. Update icon: Use Icons.bar_chart or Icons.analytics for Statistics
5. Update semantic label to 'Statistiche'

Specific changes:
```dart
// REMOVE this import:
import 'package:app_prenotazioni/features/platforms/presentation/pages/platforms_list_page.dart';

// ADD this import:
import 'package:app_prenotazioni/features/statistics/presentation/pages/statistics_page.dart';

// In _pages list, REPLACE:
const PlatformsListPage(),
// WITH:
const StatisticsPage(),

// In BottomNavigationBar items, REPLACE:
BottomNavigationBarItem(
  icon: Icon(Icons.hotel, semanticLabel: 'Piattaforme'),
  label: 'Piattaforme',
),
// WITH:
BottomNavigationBarItem(
  icon: Icon(Icons.bar_chart, semanticLabel: 'Statistiche'),
  label: 'Statistiche',
),
```

NOTE: PlatformsListPage navigation will be moved to Settings in Wave 3 (Plan 03).
  </action>
  <verify>
    <automated>flutter test test/core/widgets/app_shell_test.dart</automated>
  </verify>
  <done>AppShell shows Statistics tab instead of Platforms, navigation works correctly</done>
</task>

<task type="auto">
  <name>Task 4: Update existing tests for new navigation structure</name>
  <files>
    test/core/widgets/app_shell_test.dart
    test/integration/dashboard_navigation_test.dart
  </files>
  <action>
Update existing tests to reflect new navigation structure.

Changes needed:
1. test/core/widgets/app_shell_test.dart:
   - Replace 'Piattaforme' with 'Statistiche' in all test expectations
   - Replace PlatformsListPage with StatisticsPage in type checks
   - Update test 'all four pages accessible' to 'all five pages accessible'
   - Update navigation test to use StatisticsPage instead of PlatformsListPage

2. test/integration/dashboard_navigation_test.dart:
   - Replace 'Piattaforme' with 'Statistiche' in all test expectations
   - Update all tabs accessible test
   - Update bottom nav label expectations

Specific test updates:
```dart
// In app_shell_test.dart:
// Replace:
expect(find.text('Piattaforme'), findsOneWidget);
// With:
expect(find.text('Statistiche'), findsOneWidget);

// Replace:
expect(find.byType(PlatformsListPage), findsOneWidget);
// With:
expect(find.byType(StatisticsPage), findsOneWidget);

// In dashboard_navigation_test.dart:
// Replace:
expect(bottomNavBar.items[3].label, 'Piattaforme');
// With:
expect(bottomNavBar.items[3].label, 'Statistiche');
```
  </action>
  <verify>
    <automated>flutter test test/core/widgets/app_shell_test.dart test/integration/dashboard_navigation_test.dart</automated>
  </verify>
  <done>All existing navigation tests pass with new Statistics tab</done>
</task>

</tasks>

<verification>
1. Run all widget tests for new components:
   ```bash
   flutter test test/features/statistics/
   ```
2. Run navigation tests:
   ```bash
   flutter test test/core/widgets/app_shell_test.dart test/integration/dashboard_navigation_test.dart
   ```
3. Manual verification: Launch app and verify Statistics tab appears in navigation
</verification>

<success_criteria>
- [ ] ComingSoonPlaceholder widget created with tests
- [ ] StatisticsPage created with placeholder content
- [ ] AppShell updated to show Statistics tab (index 3)
- [ ] Platforms tab removed from bottom navigation
- [ ] All navigation tests updated and passing
- [ ] Default tab remains Dashboard (index 0)
</success_criteria>

<output>
After completion, create `.planning/phases/10-ui-ux-restructuring/10-01-SUMMARY.md`
</output>
