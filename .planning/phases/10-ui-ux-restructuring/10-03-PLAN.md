---
phase: 10-ui-ux-restructuring
plan: 03
type: execute
wave: 3
depends_on: [10-01]
files_modified:
  - lib/core/presentation/pages/settings_page.dart
autonomous: true
requirements: [UI-12]
user_setup: []

must_haves:
  truths:
    - "User can access Platforms management from Settings page"
    - "Settings has new 'Gestione' section between 'Aspetto' and 'Dati'"
    - "'Gestione' section contains 'Piattaforme di prenotazione' tile"
    - "Tapping 'Piattaforme di prenotazione' navigates to PlatformsListPage"
  artifacts:
    - path: "lib/core/presentation/pages/settings_page.dart"
      provides: "Settings with new Gestione section"
      contains: "Gestione"
      contains: "Piattaforme di prenotazione"
  key_links:
    - from: "lib/core/presentation/pages/settings_page.dart"
      to: "PlatformsListPage"
      via: "Navigator.push"
      pattern: "PlatformsListPage"
---

<objective>
Add Platforms management to Settings page under new "Gestione" section.

Purpose: Move Platforms from dedicated tab to Settings to make room for Statistics tab.
Output: Updated SettingsPage with new "Gestione" section containing Platforms tile.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-ui-ux-restructuring/10-CONTEXT.md

<interfaces>
<!-- Current Settings structure -->
From lib/core/presentation/pages/settings_page.dart:
```dart
// Current sections:
// 1. Aspetto (Theme)
// 2. Dati (Backup)
// 3. Informazioni (Version, Sviluppato con Flutter)
```

<!-- Target structure from CONTEXT.md -->
```
Impostazioni
├── Aspetto
│   └── Tema
├── Gestione ← NEW SECTION
│   └── Piattaforme di prenotazione → [PlatformsListPage]
├── Dati
│   └── Backup e Ripristino
└── Informazioni
    ├── Versione
    └── Sviluppato con Flutter
```

<!-- PlatformsListPage import -->
From lib/features/platforms/presentation/pages/platforms_list_page.dart:
```dart
class PlatformsListPage extends ConsumerStatefulWidget {
  const PlatformsListPage({super.key});
  // ...
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Gestione section with Platforms tile to SettingsPage</name>
  <files>lib/core/presentation/pages/settings_page.dart</files>
  <behavior>
    - Test 1: Settings page shows "Gestione" section header
    - Test 2: "Gestione" section appears between "Aspetto" and "Dati"
    - Test 3: "Piattaforme di prenotazione" tile exists in Gestione section
    - Test 4: Tapping tile navigates to PlatformsListPage
    - Test 5: Tile has correct icon (Icons.hotel_outlined or similar)
    - Test 6: Tile has subtitle describing its purpose
  </behavior>
  <action>
Update SettingsPage to add new "Gestione" section with Platforms management.

Changes needed:
1. Add import for PlatformsListPage:
   ```dart
   import 'package:app_prenotazioni/features/platforms/presentation/pages/platforms_list_page.dart';
   ```

2. Add new section between "Aspetto" and "Dati" (after line 32, before line 35):
   ```dart
   // After Aspetto section (theme tile + divider)
   const Divider(),

   // NEW: Management section
   _buildSectionHeader(context, 'Gestione'),
   ListTile(
     key: const Key('platforms_tile'),
     leading: const Icon(Icons.hotel_outlined),
     title: const Text('Piattaforme di prenotazione'),
     subtitle: const Text('Gestisci le piattaforme di prenotazione'),
     trailing: const Icon(Icons.chevron_right),
     onTap: () {
       Navigator.push(
         context,
         MaterialPageRoute(
           builder: (context) => const PlatformsListPage(),
         ),
       );
     },
   ),

   const Divider(),

   // Data section continues...
   ```

3. Complete updated structure:
   ```dart
   body: ListView(
     key: const Key('settings_list'),
     children: [
       // Appearance section
       _buildSectionHeader(context, 'Aspetto'),
       ListTile(
         key: const Key('theme_tile'),
         leading: const Icon(Icons.palette_outlined),
         title: const Text('Tema'),
         subtitle: Text(_getThemeLabel(themeMode)),
         trailing: const Icon(Icons.chevron_right),
         onTap: () => _showThemeDialog(context, ref),
       ),

       const Divider(),

       // NEW: Management section
       _buildSectionHeader(context, 'Gestione'),
       ListTile(
         key: const Key('platforms_tile'),
         leading: const Icon(Icons.hotel_outlined),
         title: const Text('Piattaforme di prenotazione'),
         subtitle: const Text('Gestisci le piattaforme di prenotazione'),
         trailing: const Icon(Icons.chevron_right),
         onTap: () {
           Navigator.push(
             context,
             MaterialPageRoute(
               builder: (context) => const PlatformsListPage(),
             ),
           );
         },
       ),

       const Divider(),

       // Data section
       _buildSectionHeader(context, 'Dati'),
       ListTile(
         leading: const Icon(Icons.backup_outlined),
         title: const Text('Backup e Ripristino'),
         subtitle: const Text('Gestisci i backup dei tuoi dati'),
         trailing: const Icon(Icons.chevron_right),
         onTap: () {
           Navigator.push(
             context,
             MaterialPageRoute(
               builder: (context) => const BackupSettingsPage(),
             ),
           );
         },
       ),

       const Divider(),

       // About section
       _buildSectionHeader(context, 'Informazioni'),
       // ... existing about tiles
     ],
   ),
   ```

4. Icon choice rationale:
   - Icons.hotel_outlined: Consistent with previous Platforms tab icon
   - Alternative: Icons.sell_outlined or Icons.category_outlined

5. Add Key for testing: Key('platforms_tile')
  </action>
  <verify>
    <automated>flutter test test/core/presentation/pages/settings_page_test.dart</automated>
  </verify>
  <done>SettingsPage has Gestione section with Platforms tile, navigation works</done>
</task>

<task type="auto">
  <name>Task 2: Create/update tests for Settings page changes</name>
  <files>test/core/presentation/pages/settings_page_test.dart</files>
  <action>
Create or update tests for the Settings page to verify new Gestione section.

Test structure:
```dart
group('SettingsPage', () {
  testWidgets('shows all sections', (tester) async {
    // Verify Aspetto, Gestione, Dati, Informazioni headers exist
  });

  testWidgets('shows Gestione section between Aspetto and Dati', (tester) async {
    // Find section headers
    // Verify order: Aspetto comes before Gestione, Gestione before Dati
  });

  testWidgets('shows Platforms tile in Gestione section', (tester) async {
    // Find 'Piattaforme di prenotazione' text
    // Verify it has icon and trailing chevron
  });

  testWidgets('tapping Platforms tile navigates to PlatformsListPage', (tester) async {
    // Tap the platforms tile
    // Verify navigation to PlatformsListPage
  });

  testWidgets('platforms tile has correct key', (tester) async {
    // Find by Key('platforms_tile')
    // Verify it exists
  });
});
```

If test file doesn't exist, create it with proper structure:
- Wrap in ProviderScope for Riverpod
- Use MaterialApp wrapper
- Add necessary mocks if needed
  </action>
  <verify>
    <automated>flutter test test/core/presentation/pages/settings_page_test.dart</automated>
  </verify>
  <done>All Settings page tests pass with new Gestione section</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete Phase 10 UI/UX Restructuring:
1. Statistics tab in bottom navigation (replacing Platforms)
2. Statistics page with "Coming Soon" placeholder
3. NextEventCountdownCard on dashboard (replacing CalendarAccessCard)
4. Platforms management moved to Settings under "Gestione" section</what-built>
  <how-to-verify>
1. Launch the app (flutter run -d chrome)
2. Verify bottom navigation shows: Dashboard, Calendario, Prenotazioni, Statistiche, Impostazioni
3. Tap on "Statistiche" tab - should show "Statistiche in arrivo..." placeholder
4. Go to Dashboard - should show "Prossimo Evento" card (or "Nessun evento programmato")
5. Verify NO "Apri Calendario" card on dashboard
6. Go to Impostazioni - should show new "Gestione" section
7. Tap "Piattaforme di prenotazione" - should navigate to platforms management
8. Test responsive layout by resizing browser window
9. Run all tests: flutter test
  </how-to-verify>
  <resume-signal>Type "approved" if all verifications pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
1. Run Settings page tests:
   ```bash
   flutter test test/core/presentation/pages/settings_page_test.dart
   ```
2. Run all tests:
   ```bash
   flutter test
   ```
3. Manual verification steps listed in checkpoint
</verification>

<success_criteria>
- [ ] Settings page has "Gestione" section
- [ ] "Gestione" section appears between "Aspetto" and "Dati"
- [ ] "Piattaforme di prenotazione" tile navigates to PlatformsListPage
- [ ] All Settings page tests pass
- [ ] Complete phase verification passes
</success_criteria>

<output>
After completion, create `.planning/phases/10-ui-ux-restructuring/10-03-SUMMARY.md`
</output>
